% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector_control_parameters.R
\name{set_spatial_emanator}
\alias{set_spatial_emanator}
\title{Parameterise an indoor spatial emanator strategy}
\usage{
set_spatial_emanator(
  parameters,
  timesteps,
  coverages,
  kse_in_a1,
  kse_in_a2,
  dfse_in_a1,
  dfse_in_a2,
  dufse_in_a1,
  dufse_in_a2,
  detse_in_a1,
  detse_in_a2
)
}
\arguments{
\item{parameters}{a list of parameters to modify}

\item{timesteps}{the timesteps at which to spray}

\item{coverages}{the proportion of the population who get emanators}

\item{kse_in_a1}{matrix of successful feeding indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{kse_in_a2}{matrix of successful feeding indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{dfse_in_a1}{matrix of fed mortality impact indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{dfse_in_a2}{matrix of fed mortality impact indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{dufse_in_a1}{matrix of unfed mortality impact indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{dufse_in_a2}{matrix of unfed mortality impact indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{detse_in_a1}{matrix of deterring impact indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}

\item{detse_in_a2}{matrix of deterring impact indoor parameters per timestep
With nrows=length(timesteps), ncols=length(species)}
}
\description{
The model will apply indoor spatial emanators
at \code{timesteps} to a random sample of the entire human population.
The sample size will be a proportion of the human population taken
from the corresponding \code{coverages}.
The sample \emph{can} contain humans who have already benefited from spatial emanators.

If a human in the sample lives in a treated house, the efficacy of the
spatial emanator will be returned to the maximum impact.

The structure for the spatial emanator model will be documented in
Kuipou et al (PI Ellie Sherrard-Smith)
}
