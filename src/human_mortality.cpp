#include "human_mortality.h"

std::vector<size_t> sample_mothers(const std::vector<bool>& sampleable,
                                   const std::vector<size_t>& groups,
                                   const std::vector<size_t>& died,
                                   RandomInterface* random) {
  std::vector<size_t> ret = died;
  return ret;
}
